# Ersätter CI (docs/RUNNER-POLICY.md §5): snabbare än en runner tack vare
# varm node_modules och varm .next/cache — och kostar noll Actions-minuter.
# Enhetstesterna (plan.md §5.1) körs före bygget: de tar sekunder och fångar
# logikfel som bygget aldrig ser.
npm run typecheck && npm run lint && npm test && npm run build || {
  echo "Checks failed — push blocked."
  exit 1
}
