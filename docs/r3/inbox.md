# Round 3 inbox

One line per item, free form. The manager turns lines into R3-<n> tickets
(plan.md §11.2). Workers never commit "while I was there" findings; they go
here.

- 2026-09-14 (manager, found during R3-5 audit) — [batch 3] `scripts/smoke-e2e.mjs` is not re-runnable against the same database: its menu-module steps assume the seeded state (menu off, no items, original slug), so a second `npm run smoke` without a fresh migrate+seed fails at "menyn syns inte utan modulen" and the following menu checks, independent of the shared login. Reproduced with the pre-R3-5 helper. Fix candidate: make the e2e reset or create its own menu fixture, or have `_run.mjs` document that a fresh seed is required.
- 2026-09-14 (manager) — the Codex sandbox on the PC cannot run `tsx` scripts (uv_os_get_passwd ENOMEM) nor reach MySQL; theme:preview and db:seed steps in prompts must be marked as manager-run.
- 2026-09-14 (manager, deploy step A) — **Hostinger's database is MariaDB 11.8, not MySQL.** `drizzle-kit migrate` fails silently there (`serial AUTO_INCREMENT` is MySQL-only, exactly as tests/smoke/README.md warned). Step A was done by hand: migration SQL applied with `serial` rewritten to `bigint unsigned AUTO_INCREMENT`, then indexes + the `__drizzle_migrations` row via phpMyAdmin (server-level import needs a leading `USE`). Ticket for batch 2: make the schema/migrations MariaDB-safe (bigint unsigned + autoincrement instead of serial, regenerate 0000 or add a MariaDB variant) and document it in README Deploy step A.
- 2026-09-14 (manager, deploy step A) — Remote MySQL from the PC: whitelist the IPv4 the MySQL server actually sees (`Access denied for user ...@<ip>` tells you), and connect to the IP `193.203.175.151` rather than `srv1719.hstgr.io`, otherwise Node goes out over IPv6 and is refused. Node's error is a misleading ETIMEDOUT; the mysql CLI shows the real 1045. Worth two lines in README Deploy.
- 2026-09-14 (Anton) — admin password reset by email (Resend) + change-password form in /admin. No email exists in the app today; reset = re-run seed. Needs RESEND_API_KEY. Batch 2 candidate.
- 2026-09-15 (manager, batch 2) — Codex sandbox sometimes saves Spanish text as ASCII with `?` for accented letters (seen in R3-10). Add `file`/grep encoding check to the revision gate; consider a unit test that scans src for `?` inside Spanish words.
- 2026-09-15 (manager, batch 2) — Prompts must list every file a text change touches (label maps in src/lib, middleware exceptions, client-form files); the worker stops correctly on the first unlisted file, each stop costs a resume.
- 2026-09-15 (manager) — [taken into batch 3, `docs/log/R3-3.md`] Batch 3 candidates from KNOWN-ISSUES/backlog: product/menu item images (migration), products_view analytics event (migration), e2e smoke idempotency, comercio menu+products decision, og:image, esbuild audit via drizzle-kit major.
- 2026-09-22 (batch 3, R3-12) — `registro` and `mi-sitio/login` rate-limit on the FIRST `x-forwarded-for` entry, which the client controls unless Hostinger's proxy overwrites it; a bot can rotate the header to skip the 5/h signup limit. Check what Hostinger's proxy sends and key on the last trusted hop instead (analytics.ts has the same pattern). **Batch 4 (R3-27, #78):** one helper `clientIpFrom()` everywhere, source set by `CLIENT_IP_SOURCE` (default still `xff-first`); open `/admin/diagnostico` on Hostinger, pick the source that shows your real IP, set it.
- ~~2026-09-22 (batch 3, R3-19) — admin's `hoursFromFormData` (src/lib/business.ts) still stores hours without validation (no midnight rule, no overlap merge); intake and owner now use `normalizeIntervals()`. One-line swap when someone touches the admin hours editor.~~ — done, R3-28 (#69).
- 2026-09-22 (batch 3, R3-23) — "tu año en cifras" shows the "Renovar por WhatsApp" button only when `NEXT_PUBLIC_SALES_WHATSAPP` is set on Hostinger; check it is before sending the first renewal message with the report link.
- ~~2026-09-22 (batch 3, R3-25) — extra pages have no JSON-LD (BreadcrumbList) and are not in the `theme:shots` QA gate; candidates if the module sells.~~ — done, R3-29 (#70).
- ~~2026-09-23 (batch 4 review) — admin business form: `servicesSchema` 80/200 vs intake/owner 120/300 so the admin can't save a customer's longer service; nested errors (`servicesJson.0.name`, `hoursJson.fri.0`) never rendered; values lost on error (use `src/lib/kept-form.ts`). → R3-39 (batch 5).~~ — done, R3-39 (batch 5).
- ~~2026-09-23 (batch 4 review) — no confirmation before "Borrar sección" (up to 40 dishes + images, `menu-actions.ts`) or deleting a dish/product/photo. → R3-40.~~ — done, R3-40 (batch 5).
- ~~2026-09-23 (batch 4 review) — `ensureRollupFresh`-style fallback in `src/lib/rollup.ts` runs once per process per day, owners' "today" goes stale; 30-day windows in `lead-queries.ts`/`queries.ts` are 31 days on the UTC `curdate()`; a valid preview link on a published site still shows "no es visible" + noindex (redirect instead); theme home pages lack `<main>`. → R3-41.~~ — done, R3-41 (batch 5).
- 2026-09-23 (batch 4 review) — `secondaryPhone` stored raw in intake/owner (zod English "Too big" past 20 chars); owner/intake `mapsUrl`/socials lack the admin's `https?://` check; logo replace leaves files on disk (`/api/upload`); `?next=` set by middleware but ignored by both logins; `saveItem` skips `MENU_MAX_ITEMS_PER_SECTION` when moving an item, and the `redirect:<slug>` tag is never revalidated; `IntakeSubmit` no pending state, `OtpButton` calls `useFormStatus` outside its form; photo cap is count-then-insert; admin site page ignores `?ok=`. → R3-42. **Batch 5 (R3-42):** done — secondaryPhone, owner/intake URLs, logo files, `?next=`, section cap on move, IntakeSubmit/OtpButton pending. Still open: the `redirect:<slug>` tag is never revalidated, the photo cap is count-then-insert, the admin site page ignores `?ok=`.
- 2026-09-23 (batch 4, R3-29) — the gastronomía "Carta" subpage reads "Carta" then "LA CARTA / La carta" (page title + the menu block's own heading); drop the block heading on subpages whose type already names it.
- 2026-09-23 (batch 4 review, ideas) — footer credit "Hecho con sitio.com.py" on customer sites (referrals + backlinks); landing showcase of real published sites; monthly WhatsApp stats message to owners (visible value all year, needs PR-17); renewal reminders at 30/15/7 days with the year-report link (PR-20); "Dejanos una reseña en Google" button when a Maps link exists; live thousands separators in price inputs; intake autosave per step; `/registro` spotting an already-registered phone and offering the existing link; payment details (bank/Tigo) in the "Tu plan" card.
- 2026-09-23 (batch 5, R3-42) — moving a dish to another section keeps its old `sort_order`, so it can land in the middle of the new section instead of at the end. Cosmetic; set `sort_order` to `nextSort` of the target section on a move.
