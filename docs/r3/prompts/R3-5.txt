Task:
Repo sitio (Next.js 15, Playwright smoke suites in tests/smoke/). Ticket R3-5, from KNOWN-ISSUES.md (O3 and S6 entries). Every smoke suite logs in to superadmin through adminLogin in tests/smoke/_lib.mjs, and the login rate limit is five attempts per email per 15 minutes in-process, so the suite count is exactly at the ceiling and a sixth suite would fail on its own login. Make adminLogin log in once per npm run smoke and reuse the session: on the first call it logs in as today and saves the Playwright storageState to a file under .smoke/ (git-ignored); later calls, in later processes, create the page from that storageState and only verify that /admin loads without redirecting to /admin/login. If the saved state is stale (redirects to login), fall back to a real login and overwrite the file. The runner tests/smoke/_run.mjs deletes the state file at the start of a run so a run never reuses a session from an earlier server. The call sites in the suites keep their current signature adminLogin(page, ok, browser) and do not change; if a signature change is unavoidable, stop and say so.

Files to touch:
tests/smoke/_lib.mjs
tests/smoke/_run.mjs
.gitignore (one line: .smoke/)
tests/smoke/README.md (two or three lines describing the shared login)

Do not touch:
tests/smoke/polish.mjs, presentation.mjs, products.mjs, radar.mjs, scripts/smoke-e2e.mjs.
src/app/admin/login/**, src/lib/rate-limit* or wherever rateLimit lives, and every other file under src/.
src/db/schema.ts, drizzle/**, src/lib/auth*.ts, src/lib/session*.ts, src/middleware.ts, src/lib/env.ts, src/lib/billing.ts, src/app/api/**, .husky/**, .env.example, anything under .github/.

Definition of done:
npm run smoke against a freshly started server performs exactly one POST to the login action for the whole run (verify by counting the login checks that print the word login in the suite output, or by a counter file; describe the method in the report).
Running npm run smoke twice in a row against the same server without restarting it stays green (previously the second run hit the rate limit).
Each suite still prints its ok login line so the output shape stays the same.
.smoke/ is git-ignored and nothing under it is committed.
Stay within the listed files. If something outside them must change to satisfy the definition of done, stop and say so instead of changing it.

Commands to run before reporting:
npm run typecheck   (working directory: repo root)
npm run lint   (working directory: repo root)
npm test   (working directory: repo root)
npm run build && PORT=3100 npm run start   (working directory: repo root, in a second terminal, against the seeded local database)
npm run smoke   (working directory: repo root, twice in a row against that server)
Run every listed command. Report FAIL with the error text rather than skipping or substituting another check.

Report format:
Files changed: each path with a one-line summary of the change.
Commands run: each command with PASS or FAIL and a one-line result.
Flagged or not done: anything skipped, blocked, ambiguous, or outside scope, or None.
Keep the report under 30 lines. No diffs, no file dumps. Never print or write secrets, tokens, or API keys.
