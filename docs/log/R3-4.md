# Round 3, batch 4

Runner: a Claude Code cloud session (2026-09-23) with
`docs/r3/prompts/batch-4.md` as its task. Anton at the keyboard for part A
(taking batch 3 live on Hostinger); the session guides and verifies from the
cloud, never asks for a secret. Part B: one branch `r3/R3-<n>` and one PR per
ticket, same revision gate as batch 3 (typecheck, lint, unit tests; build when
a route/config moves; smoke on a fresh local DB when admin/intake/owner
panel/upload moves). Migrations only locally, never to Hostinger.

## Tickets

| Ticket | PR | Gate | Notes |
|---|---|---|---|
| R3-26 `/admin/diagnostico` | #68 | typecheck, lint, 338 tests (2 new: `diagnostics`), build; fresh MariaDB 10.11 + built server: `npm run smoke` 8/8 green (210 ✓ lines) **twice in a row** — products.mjs: superadmin sees headers/clock/uploads/env, `CRON_SECRET` shows "sí" and never its value, noindex, owner and anonymous are redirected to `/admin/login` | No migration. `src/lib/diagnostics.ts`: proxy headers + what `clientIp()` returns, `version()`/`now()`/`utc_timestamp()`/`@@time_zone` (raw strings, not through the pool's `Z`) with the offset in hours, `UPLOADS_DIR` exists/writable (temp file written and deleted)/outside the app dir, env presence yes/no only. Page is `force-dynamic`, noindex, linked from the admin header. Unit test count before this batch is 336, not the 342 in R3-3's close |
| R3-28 admin hours through `normalizeIntervals()` | #69 | typecheck, lint, 341 tests (3 new + 1 extended), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (210 ✓) | No migration. `hoursFromFormData` (admin) uses the same cleanup as intake and owner panel: invalid times dropped, `00:00` close = midnight, overlaps merged, sorted. Admin `hoursSchema` refine also accepted only `open < close` as strings, so 19:00–00:00 failed with an error the form never showed (found by this batch's review) — now allows a `00:00` close. `hours.ts` has its own day order so the new `business → hours` import doesn't cycle at load time |
| R3-29 extra pages polish | #70 | typecheck, lint, 343 tests (2 new: `breadcrumbJsonLd`), build; `npm run theme:preview` + `theme:shots` write `<tema>-v1-pagina.html` and its 3 widths for all four themes, 360 px gastronomía checked by eye | No migration. `breadcrumbJsonLd()` (home → page, absolute URLs) rendered through `jsonLdHtml()` on `/[slug]/[page]` and its preview. `theme:preview` renders `RenderSubPage` once per theme with a different page type each (servicios → servicios, gastronomía → carta, comercio → productos, salud → contacto) and a second pill; `theme:shots` picks the files up by name. The HTML shell is now one `pageHtml()` |
| R3-31 routing: admin/owner prefix, matcher, reserved slugs, salud preview CSS | #71 | typecheck, lint, 350 tests (7 new: `route-area`), build; built server: `/administradora-lopez` 200 (was a redirect to `/admin/login`), `/medialunas-dona?preview=<token>` renders the preview (the matcher skipped it), `/admin`, `/admin/diagnostico`, `/mi-sitio` still redirect anonymous; fresh MariaDB: `npm run smoke` 8/8 green (210 ✓) | Found by this batch's review. Middleware matched `startsWith("/admin")`/`"/mi-sitio"`, so any customer slug starting with those words was behind a login (and still in the sitemap); now `routeArea()` (`src/lib/route-area.ts`) matches whole segments. Matcher excluded `api`/`media` without a slash, so slugs like `medialunas-…`/`apicultura-…` never got the `?preview=` rewrite. `registro`, `opengraph-image`, `twitter-image` added to reserved slugs (static routes shadow them). Preview layout now imports `salud.css` — clinics saw an unstyled preview |
| R3-32 payment receipts over 1 MB + guaraní amounts with dots | #72 | typecheck, lint, 368 tests (18 new: `parseGs`, schemas), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (212 ✓) **twice in a row**; reproduced first: with `main`'s config the e2e owner report with a 2 MB+ receipt fails and the server logs `Body exceeded 1 MB limit` | Found by this batch's review. Receipts go through server actions and Next's default body limit is 1 MB, so a normal phone photo of a transfer crashed "Informar pago" (and the admin's payment form) before `receipt.ts` could answer — `experimental.serverActions.bodySizeLimit: "11mb"`. `z.coerce.number("300.000")` stored ₲300; menu/product prices stripped every non-digit ("35 mil" ⇒ ₲35, "15.000,00" ⇒ ₲1.500.000). New `parseGs()` in `format.ts`: dots only as 3-digit groups, `Gs`/`₲`/spaces stripped, `,00` allowed, anything else is an error; empty is an error for payments (was 0). e2e: owner types "300.000" with a 2 MB+ receipt, admin row shows 300.000 |
| R3-33 intake link hardening | #73 | typecheck, lint, 368 tests, build; fresh MariaDB + built server: `npm run smoke` 8/8 green (214 ✓) **twice in a row** — e2e: superadmin session + intake token uploads to the token's business (200; was 400 "Ogiltigt businessId"), the customer switches to another number and the admin's code answers "no hay un código activo", switching back verifies | Found by this batch's review. `getIntakeSession` accepts only `draft` (was: everything but `archived`) — publishing before the customer submitted left a live link that could rewrite name/phone/hours on a published site and send it back to review. `verifyCodeAction` requires `verifications.phone` = the business's current number (a code sent to A verified B). `/api/upload`: a `token` field takes the token path even with a session, so an owner still logged in on their phone who fills in an intake for a second business doesn't upload to the first one |
| R3-34 sessions checked against the account on every request | #74 | typecheck, lint, 368 tests, build; fresh MariaDB + built server: `npm run smoke` 8/8 green (217 ✓) **twice in a row** — products.mjs: admin disables the owner ⇒ the same cookie is sent to `/mi-sitio/login` and `/api/upload` answers 401; re-enabled ⇒ in again without a new login | Found by this batch's review. `currentUser()` trusted the iron-session cookie alone for 30 days: a disabled owner, or the previous owner after the WhatsApp number moved the login to someone else, kept editing, uploading and reporting payments. Now `sessionStillValid()` (React `cache`, one lookup per request) requires an active user with the same role, and for owners that `businesses.owner_user_id` still points at them. Every guard (requireRole, ownerContext, upload route, login pages) goes through it. `setOwnerStatusAction` now only touches `role = 'owner'` rows (an id in the form could disable a superadmin) |
| R3-35 "Abierto ahora" computed in the browser + 24 h JSON-LD | #75 | typecheck, lint, 374 tests (6 new: the browser script against `openState`/`statusText` every 37 min over a week for four hour maps, plus 24 h spec), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (219 ✓) **twice in a row** — csp.mjs fakes the clock (Monday closed, Tuesday lunch open) and reads the pill after hydration; 0 CSP violations | Found by this batch's review. The pill was computed at render time on ISR pages (revalidate 3600, stale-while-revalidate): a quiet site could say "Abierto ahora · cierra 18:00" at 23:00. `StatusPill` now carries `data-hours`, and `OpenNowScript` (core in `src/lib/open-now-script.ts`) recomputes it in the browser before hydration; `suppressHydrationWarning` on the text and dot, otherwise React puts the server text back (seen on the first try). The server text stays as the no-JS fallback. `statusText()` moved to `src/lib/hours.ts`. `openingHoursSpecification`: 00:00–00:00 now `closes: "23:59"` (Google read it as closed all day) |
| R3-36 billing: next-period defaults, no auto-republish of hand-paused sites, Asunción day | #76 | typecheck, lint, 376 tests (2 new: `todayAsuncion`, lifecycle late evening), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (220 ✓) **twice in a row** — e2e checks the admin payment form defaults to the next period; ad-hoc script against the built server: a site paused by hand stays paused after a confirmed payment, a site paused by the lifecycle is published again | Found by this batch's review. The admin payment form prefilled the *current* period, so confirming a renewal left `expires_at` unchanged and the lifecycle paused a paid site later — now `ownerReportPeriod()` (same rule as the owner's own report). `confirmPaymentAction` republished any paused site; now only when the latest `status_paused` log has `reason: subscription_expired`. Billing "today" was the UTC day (after 21:00 in Asunción it was tomorrow: grace/expired and pauses a day early when run from `/admin/pagos` in the evening) — new client-safe `todayAsuncion()` is the default in `daysUntil`/`lifecycleStatus`/`ownerReportPeriod` and used by the lifecycle, the site page, `/admin/pagos` and `/admin/promociones` |
| R3-37 single-use reset links, owner media delete, atomic OTP attempts | #77 | typecheck, lint, 377 tests (1 new + 4 updated: password version), build; built server, ad hoc: a reset link shows the form, resets, and the same link then says "no es válido"; fresh MariaDB: `npm run smoke` 8/8 green (220 ✓) — the e2e OTP steps (wrong code rejected, right code logs in/verifies) go through the new claim | Found by this batch's review. Reset tokens were stateless for 30 min and reusable after a reset; claims now carry `pv` = HMAC of the current password hash (not the hash), checked on the page and in the action, and the update has the old hash in its WHERE. `ownerDeletePhotoAction` deleted any media row of the tenant (payment receipts, dish/product images) — now only `photo`/`logo`. OTP (owner login + intake): the attempt is claimed with `UPDATE … WHERE attempts < 5 AND verified_at IS NULL` before comparing (parallel requests got past the cap), and `verified_at` is set only if still null (a code works once) |
| R3-27 one client-IP helper, source switchable (default unchanged) | #78 | typecheck, lint, 381 tests (4 new: `client-ip`), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (220 ✓) — registro/promo still get their per-run IP | **Half done by design:** Anton hasn't read the headers yet (part A5 not run). Five copies of "first x-forwarded-for entry" (registro, owner login, admin login, password reset, analytics) are now one `clientIpFrom()` in `src/lib/client-ip.ts`. `CLIENT_IP_SOURCE` = `xff-first` (default, today's behaviour) / `xff-last` / `x-real-ip`, documented in `.env.example` and README. `/admin/diagnostico` shows what each source returns for the current request. Tomorrow: open the page on Hostinger (optionally with a made-up `x-forwarded-for` from curl), pick the source that shows the real IP, set the env var — or change the default in code |
| R3-38 customer forms keep what was typed after a server error | #79 | typecheck, lint, 385 tests (4 new: `kept-form`), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (222 ✓) **twice in a row** — e2e: intake with a too-short description shows the error and still holds "Pan rico" + the first service; products.mjs: a product priced "35 mil" is refused and the form still holds the name and the price typed | Found by this batch's review. React 19 resets a `<form action>` after every action, errors included, to the fields' `defaultValue` (the saved data), so a customer who typed a description on the phone and got "mínimo 80 caracteres" lost everything; R3-32 made price errors more common. `src/lib/kept-form.ts`: `keepSubmittedOnError()` wraps the action on the client and keeps the submitted `FormData` in state on error only (never sent back to the server); `kept`/`keptServices`/`keptHours` feed the `defaultValue`s. Wired into the intake data form, the owner business form, the menu item form and the product form. Not the admin business form (→ R3-39 in batch 5). The wrapper means these four forms need JS (they already did for uploads) |

Order note: R3-26 ran first (part A needs it). Part A then stopped after
step 1 was handed to Anton — he went offline for the night and asked the
session to "build as far as you can, find bugs and ideas, merge when green".
R3-30 was not started (no answer in `decisions-needed.md`), and R3-27 was
built without the Hostinger headers (helper + switch, default unchanged).
R3-31…R3-38 are new tickets from a read-only review of the whole app (three
Opus subagents, model set explicitly, no Fable), each finding verified in
the code before it became a ticket.

## Review findings (batch 4, 2026-09-23)

Fixed in this batch: middleware prefix match on `admin`/`mi-sitio` slugs,
matcher `api`/`media` prefixes, unreserved `registro`, salud preview CSS
(R3-31); receipts over 1 MB, "300.000" = ₲300, "35 mil" = ₲35 (R3-32); intake
links live on published sites, OTP not bound to the phone, token vs session
in upload (R3-33); disabled/re-linked owners keep their session, status
action could hit a superadmin (R3-34); "Abierto ahora" frozen in ISR, 24 h
JSON-LD (R3-35); admin payment form prefilled the current period, confirming
a payment republished hand-paused sites, UTC billing day (R3-36); reusable
reset links, owners deleting receipts, OTP attempt race (R3-37); forms wiping
input on error (R3-38); admin hours refine rejected 19:00–00:00 (R3-28).

Not fixed yet — tickets R3-39…R3-43 in `docs/r3/prompts/batch-5.md`, one
line each in `docs/r3/inbox.md`:

- Admin business form: service limits 80/200 vs 120/300 elsewhere (the admin
  can't save what the customer wrote), nested errors never rendered, values
  lost on error.
- No confirmation before "Borrar sección" (up to 40 dishes + images) or
  deleting a dish/product/photo.
- Rollup fallback once per process per day (owners' "today" stale); "30 d"
  windows are 31 days on the UTC date; a preview link on a published site
  still says "no es visible" and is noindex; theme home pages lack `<main>`.
- `secondaryPhone` unvalidated in intake/owner; owner/intake `mapsUrl` and
  socials accept non-URLs; replacing the logo leaves its files on disk;
  `?next=` ignored after login; moving a dish ignores the per-section cap;
  `IntakeSubmit`/`OtpButton` show no pending state; photo cap raceable by a
  script; the admin site page ignores `?ok=`.
- Questions for Anton (`docs/decisions-needed.md`): Swedish text in the
  Spanish admin; trial starting at registration rather than publication.

Checked and fine (from the review): hidden menu items/products/pages don't
leak, sitemap filtering, noindex on preview/report, cache revalidation on
edits and pauses, wa.me formatting, MariaDB compatibility of the SQL.

## Batch close

13 PRs merged (#68–#79 plus this one): 1 planned ticket for part A (R3-26),
3 planned part-B tickets (R3-27 half, R3-28, R3-29), 8 from the review
(R3-31…R3-38). No migration — `hostinger-import-0001-0003.sql` is still the
only import. Unit tests 336 → 385, smoke 8 suites, 204 → 222 ✓ lines, green
twice in a row against the same MariaDB 10.11 database after every ticket
that touched smoke files. One local incident: mariadbd died once mid-batch
(every login ECONNREFUSED); restarted on the same datadir, reran — noted in
the batch-5 prompt.

Still blocked on Anton: everything in "Live steps" below; R3-30 (`comercio`
menu vs products); choosing `CLIENT_IP_SOURCE` (needs `/admin/diagnostico`
on Hostinger); PR-17 WhatsApp Cloud API (Meta verification) and the
reminders half of PR-20; PR-21 R2 (only if the uploads-persistence test
fails); the two new questions in `docs/decisions-needed.md`.

## Live steps

**None run.** Step A1 (import `docs/deploy/hostinger-import-0001-0003.sql`)
was handed to Anton with instructions; he went offline before answering.
Everything from R3-3's "Live steps for Anton" is still open, plus from this
batch: open `/admin/diagnostico` after the deploy and set `CLIENT_IP_SOURCE`.
The whole walk-through, in order, is part A of `docs/r3/prompts/batch-5.md`.

