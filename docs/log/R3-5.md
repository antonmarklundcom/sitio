# Round 3, batch 5

Runner: a Claude Code cloud session (2026-09-23) with
`docs/r3/prompts/batch-5.md` as its task. Part A: Anton at the keyboard takes
batches 3 + 4 live on Hostinger (import, env vars, redeploy, live checks,
`/admin/diagnostico` → `CLIENT_IP_SOURCE`, open questions); the session guides
one step at a time and verifies from the cloud, never asks for a secret.
Part B: one branch `r3/R3-<n>` and one PR per ticket, same revision gate as
batches 3 and 4 (typecheck, lint, unit tests; build when a route/config moves;
smoke on a fresh local DB when admin/intake/owner panel/upload moves).
Migrations only locally, never to Hostinger.

## Tickets

| Ticket | PR | Gate | Notes |
|---|---|---|---|
| R3-39 admin business form | #82 | typecheck, lint, 388 tests (3 new: `servicesSchema` 120/300, `businessFieldErrors`), build; fresh MariaDB 10.11 + built server: `npm run smoke` 8/8 green (227 ✓) **twice in a row** — e2e: admin saves a 110-char service name (public site shows it); a 301-char description (maxlength removed) shows "Servicio 1 («…»): Max 300 tecken." and the typed name + service are still in the form | No migration. `servicesSchema` 80/200 → 120/300 like intake/owner, inputs `maxLength` too. New `businessFieldErrors()` (`src/lib/business.ts`) folds nested zod paths into `servicesJson` ("Servicio N («name»): …") and `hoursJson` ("Viernes: …"); the hours error is now rendered under the editor. The admin form wraps its action in `keepSubmittedOnError` and rebuilds its defaults from the submitted FormData; `ServicesEditor`/`HoursEditor`/`PresentationBlock` hold state, so they get a per-error `key` to remount with the typed values. Local: mariadbd died once before the first smoke (ECONNREFUSED on every login), restarted detached on the same datadir |
| R3-40 second tap before destructive owner actions | #83 | typecheck, lint, 388 tests, build; fresh MariaDB + built server: `npm run smoke` 8/8 green (230 ✓) **twice in a row** — e2e: "Borrar sección" first tap leaves the section, "Sí, borrar la sección y sus N platos" deletes it (dish image 404); products.mjs: owner "Borrar" asks first for both products, admin deletes via the confirm label | No migration. New `ConfirmSubmit` (`src/components/mi-sitio/confirm-submit.tsx`): first tap `preventDefault` + label "Sí, borrar …", second tap submits, back to normal after 5 s. Used for section (label names the dish count), dish, product and photo in the owner panel — the same components serve the admin's owner-module cards. No modal library; without JS the form submits directly (panel needs JS anyway). Admin "Borrar página" left as is (admin-only, not in the ticket) |
| R3-41 small correctness batch | #84 | typecheck, lint, 392 tests (4 new: `lazyRollupDue`, `windowStartAsuncion`), build; `npm run theme:preview` + `theme:shots` for all four themes (360 px gastronomía checked by eye, layout unchanged); fresh MariaDB + built server: `npm run smoke` 8/8 green (232 ✓) **twice in a row** — e2e: a preview link on published business 1 answers 307 to the public slug, the home page has exactly one `<main>`; presentation.mjs: a draft's preview still shows "Vista previa" | No migration. Lazy rollup runs at most every 10 min per process instead of once per day (`lazyRollupDue`, `src/lib/rollup.ts`), so owners' "hoy" moves during the day. The six "30 días" subqueries in `lead-queries.ts`/`queries.ts` use `d.day >= windowStartAsuncion(30)` (today + 29 days on the Asunción day) instead of `curdate() - interval 30 day` (31 UTC days). `/preview/[slug]` and `/preview/[slug]/[page]` redirect (307, the site can be paused again) to the public URL when the site is published — `site.pages` is the same selection the public subpage uses. The four theme home components wrap everything between the hero `<header>` and the closing/footer in `<main>`, like the subpages |
| R3-42 input hygiene | #85 | typecheck, lint, 400 tests (8 new: `safeNext` incl. `//`, `/\`, schemes, backslashes, control chars, other area, `..`; `optionalPyPhoneEs`/`optionalHttpUrlEs`; `ownerFormSchema`), build; fresh MariaDB + built server: `npm run smoke` 8/8 green (240 ✓) **twice in a row** — e2e: logo replaced twice, each old logo file 404 and the new one 200; anonymous `/admin/pagos` → login with `next` → back on `/admin/pagos`; `next=//evil.example`, `/\evil.example`, `https://evil.example/admin` all land on `/admin` | No migration. `src/lib/form-fields.ts`: `secondaryPhone` through `normalizePyPhone` (stored as +595…, Spanish error) in intake and owner; owner `mapsUrl` + socials and intake socials must start with `http(s)://` ("Pegá el enlace completo, por ejemplo …"), placeholders added. Logo replace deletes the old logo rows' files (`/api/upload`). `src/lib/safe-next.ts`: `?next=` honoured after admin and owner login (also when already logged in) only for a same-origin relative path in the login's own area. `saveItem` checks the target section's 40-dish cap when a dish moves. `IntakeSubmit` and `OtpButton` use `useActionState`'s pending flag (the latter called `useFormStatus` outside its form). A finding I logged mid-ticket (media files shared by content hash) was wrong — the hash has a random salt — and was withdrawn before commit. Left in the inbox: `redirect:<slug>` tag revalidation, photo-cap race, admin `?ok=`, moved dish keeps its old sort order |

## Live steps

| Step | Result | Notes |
|---|---|---|
