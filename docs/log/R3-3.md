# Round 3, batch 3

Runner: a Claude Code cloud session that Anton opened with the whole batch
as its task (2026-09-22). No Codex on the PC this time, so there are no Codex
session ids; the session wrote, verified and merged each ticket itself, one
branch `r3/R3-<n>` and one PR per ticket, same revision gate as §11.2
(typecheck, lint, unit tests; build when a route/config moves; smoke on a
fresh local MySQL 8.4 when admin/intake/owner panel/upload moves).
Migrations are generated and applied only to the local MySQL — **never to
Hostinger**; every ticket that ships one says so and Anton imports it.

## Audit (before the first ticket)

Every `KNOWN-ISSUES.md` / plan §10 / inbox item was checked against the code
on `main` (fc9e032). Stale entries removed from the docs:

- Smoke login budget at the cap — fixed by R3-5 (`storageState` in `_lib.mjs`).
- `BusinessFormDefaults` dead `themeKey`/`paletteVariant` — fixed by R3-2.
- `seed-dev.ts` variants 1/2/3 — fixed by R3-4 (`presentationFor`).
- `ThemePalettes` trim — fixed by R3-1. PR-19a self-signup — R3-9.
- "Item images need a migration" — wrong: `media.kind` has `menu_item` /
  `product` and both tables have `media_id` since 0000.
- `tests/smoke/README.md` "MariaDB duger inte" — stale since R3-6.
- PR #50 (themes v2 "Placa") had no log; `docs/log/placa.md` written.
- New, not in any doc: `tests/smoke/registro.mjs` also fails on a second run
  (in-process rate limit 5/h per IP), next to the known e2e menu failure.
- Missed by the earlier audit: PR-19b (self-reported payments) is still open.

## Tickets

| Ticket | PR | Gate | Notes |
|---|---|---|---|
| R3-12 smoke re-runnable against the same DB | #52 | typecheck, lint, 298 tests; fresh MySQL 8.4 + built server: `npm run smoke` twice in a row, 7/7 suites green both times (151 checks) | e2e menu block normalises module state, uses per-run names scoped to its own section and deletes it afterwards; e2e deletes the run's photos on business 1 (they grew +4/run until the 20-photo cap broke the tenant check); registro/promo send a per-run `x-forwarded-for`. Still in-process: a 3rd run within 15 min hits the owner-code limit (4/number), and a crashed run leaves a pending code for 10 min — documented in tests/smoke/README.md. XFF spoofing of rate limits → inbox |
| R3-13 unused template SVGs + per-theme demo menu | #54 | typecheck, lint, 298 tests; `npm run theme:preview` writes all 12 pages, salud shows "Consultas / Consulta general", gastronomía keeps the dishes | `public/file.svg`, `globe.svg`, `window.svg` deleted (no references). `DEMO_MENU` → `DEMO_MENUS` keyed by theme (gastronomía dishes, salud consultations, servicios visits/jobs, comercio stock/encargo), each keeping the three layout-breaking cases (no description, no price, long name) |
| R3-14 landing `og:image` drawn in code | #55 | typecheck, lint, 298 tests, build; live on the built server: `/` has `og:image` + `twitter:card summary_large_image` pointing at `/opengraph-image` (200, 1200×630 PNG, checked by eye), a published site without hero has no `og:image` | `src/app/opengraph-image.tsx` with `next/og` ImageResponse in the landing palette, default font, "Gs." (₲ is missing from the font). Root layout got `metadataBase` from `baseUrl()` — without it the card URL was `localhost:3000`. `siteMetadata` now always sets `openGraph.images` (hero or `[]`) so customer sites never inherit the sitio card |
| R3-15 `npm audit` esbuild findings | #56 | typecheck, lint, 298 tests; `npm audit` (dev deps included) 0 vulnerabilities; `npm run db:generate` "No schema changes"; `npm run db:migrate` on a fresh MySQL 8.4 database applies 0000 + 0001 (17 tables) | `drizzle-kit` ^0.31.10 → ^0.31.11 (latest stable). Every 0.x still declares `@esbuild-kit/esm-loader` (1.0 is an rc with a new migration layout — not taken), but the kit's bin never requires it, so `overrides` pins its nested esbuild to ^0.25.12, the same version the kit uses itself |
| R3-16 menu item + product images | #57 | typecheck, lint, 301 tests (3 new: `itemImage`), build, `npm run theme:preview`; fresh MySQL 8.4 + built server: `npm run smoke` 7/7 suites green (164 ✓ lines), products suite +12 checks, e2e +3 | No migration. `/api/upload` takes `menu_item`/`product` with a `targetId` for a session (never an intake token); `itemImageTarget()` checks tenant + module before sharp runs, else 404. One image per row: upload sets `media_id` and deletes the old media + files, "Quitar foto" removes it, deleting an item/section/product deletes its media. Queries left-join media (tenant in the join) into `image: ItemImage \| null`; `SiteMenu` shows a 72px thumbnail, `SiteProducts` a 4:3 edge-to-edge card image. Smoke: products.mjs uploads/replaces/removes/deletes and checks 404 on the old files and a foreign `targetId`; e2e checks a dish image is removed with its section; products.mjs now deletes its two products (was +2/run towards the cap of 60). Also fixed a smoke flake: polish.mjs's `!body.includes('500')` matched a random payment reference `OP-500257` |
| R3-17 `products_view` analytics event | #58 | typecheck, lint, 301 tests, build; `db:migrate` applies 0002 on top of an existing 0000+0001 DB and on a fresh one; fresh MySQL 8.4 + built server: `npm run smoke` 7/7 green (165 ✓ lines), `analytics_events` gets a `products_view` row from the browser | **Migration `drizzle/0002_aspiring_rhodey.sql`** (enum on `analytics_events.type`, one `ALTER … MODIFY`) — generated and applied locally only; Anton imports it on Hostinger. `EVENT_TYPES` + schema enum, `SiteProducts` gets `data-ev-view="products_view"` (same beacon as `menu_view`, ingest only — no rollup column, like menu/gallery). Smoke asserts the attribute and, via an init script wrapping `sendBeacon` (Playwright can't see beacon bodies), that the browser actually sends it. Admin module text for products updated (was "solo en comercio") |
| R3-18 per-CTA analytics column | #59 | typecheck, lint, 303 tests (2 new: `ctaLocation`), build; `db:migrate` applies 0003 on the previous DB and on a fresh one; fresh MySQL 8.4 + built server: `npm run smoke` 7/7 green (166 ✓ lines); `analytics_events` rows `whatsapp_click/hero`, `products_view/catalogo`, `page_view/NULL` | **Migration `drizzle/0003_futuristic_randall_flagg.sql`** (`analytics_events.cta_loc varchar(32)` nullable) — local only, Anton imports it. Beacon sends `l` (the `data-ev-loc`, or the section id for a view event); ingest keeps it only if it matches `^[a-z0-9_-]{1,32}$` (`ctaLocation`). Admin site page: "Clics por botón, 30 días" from raw events (bots excluded, like the rollup), with the loc names in Spanish; the owner panel is unchanged (no extra query there). Smoke: e2e beacon with `l:'hero'` → "WhatsApp · portada" in admin; products.mjs checks the browser's `products_view` carries `l=catalogo` |
| R3-19 split opening hours (siesta) in intake | #60 | typecheck, lint, 309 tests (6 new), build; fresh MySQL 8.4 + built server: `npm run smoke` 7/7 green (168 ✓ lines) — e2e splits Monday in the intake, the DB holds `08:00–12:00, 15:00–19:00` and the form shows both again; intake screenshot at 360/900 px checked | `normalizeIntervals()` in `src/lib/hours.ts`: valid `HH:MM` only, `00:00` close = midnight, sorted, overlapping/touching shifts merged, max 2. Intake: per-day "Corta al mediodía" button reveals a "tarde" shift (`hours.<day>.1.*`, first shift keeps its old field names) and cuts a pre-filled morning to 12:00 so it doesn't merge back. `ownerHoursFromForm` uses the same normalizer — fixes a latent owner-panel bug where `19:00–00:00` was dropped (string compare `"00:00" > "19:00"`). Admin `hoursFromFormData` untouched |
| R3-20 superadmin edits owner menu/products, actor-logged | #61 | typecheck, lint, 309 tests, build; fresh MySQL 8.4 + built server: `npm run smoke` 7/7 green (173 ✓ lines) — products.mjs: superadmin adds a product from `/admin/sitios/1`, it shows on the public site and in the owner's panel, then deletes it; `activity_log` has `admin_product_added/deleted` with the superadmin as actor next to the owner's `owner_product_*` | Every menu/product action is now an internal implementation taking an `EditorContext` (`owner-context.ts`: owner from the session, or superadmin + a businessId bound with `.bind` on the admin page — never a form field; `adminEditorContext` requires the role). Owner exports keep their signatures; 14 `admin…Action` exports added. `afterWrite` logs `admin_…` instead of `owner_…` for superadmin and revalidates the admin page. The admin site page embeds the same `OwnerMenu`/`OwnerProducts` (new optional `heading`, `businessId` for image uploads) in `.panel--embedded` when the module is on — one editor, not two that drift apart |

Paused 2026-09-22 after R3-12 (Anton turned off his PC). Handoff prompt for
the rest of the batch: `docs/r3/prompts/batch-3-continue.md`.

## Live steps for Anton (added by this batch)

Nothing below has been run against Hostinger. Import in order, in phpMyAdmin
(with a leading `USE <db>;` for a server-level import), after the batch-2
step `drizzle/0001`:

- `drizzle/0002_aspiring_rhodey.sql` — R3-17, adds `products_view` to
  `analytics_events.type`. Then its journal row (hash = sha256 of the file,
  checked against a local `db:migrate`):
  `INSERT INTO __drizzle_migrations (hash, created_at) VALUES ('bbd9822c450ce9d66bd87529b8b89d14b94cf305dfe1dc4fc87c18c7d628ec7b', 1790115212529);`
- `drizzle/0003_futuristic_randall_flagg.sql` — R3-18, adds
  `analytics_events.cta_loc varchar(32)`. Journal row:
  `INSERT INTO __drizzle_migrations (hash, created_at) VALUES ('d389fa4d6c0affbe4254210fa4d6893b1e12b3fa9ca538de0eaf654507aff829', 1790116229456);`
