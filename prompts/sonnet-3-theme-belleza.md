# Phase S3 — Theme `belleza`. CANCELLED 2026-09-13.

Do not run this prompt. `belleza` is a category, not a theme (plan §1.12):
it renders on theme `salud` with palette variant 2 (rose), locked by
`presentationFor` (S7, `src/lib/presentation.ts`). `salud` itself is built by
S2 (`prompts/sonnet-2-theme-salud.md`, merged).

See `plan.md` phase table and §1.12 for the full reasoning.
