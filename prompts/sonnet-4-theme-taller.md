# Phase S4 — Theme `taller`. CANCELLED 2026-09-13.

Do not run this prompt. `taller` is a category, not a theme (plan §1.12):
it renders on theme `servicios` with palette variant 1 (orange), locked by
`presentationFor` (S7, `src/lib/presentation.ts`). `servicios` was already
built in round 1.

See `plan.md` phase table and §1.12 for the full reasoning.
